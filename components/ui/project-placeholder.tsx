'use client';

export default function ProjectPlaceholder() {
  return (
    <div className="w-full h-full bg-gradient-to-br from-[hsl(var(--primary)_/_0.1)] to-[hsl(var(--secondary)_/_0.1)] flex items-center justify-center">
      <div className="text-[hsl(var(--primary))] opacity-50">Coming Soon</div>
    </div>
  );
}
